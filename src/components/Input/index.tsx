import InputError from "./InputError";
import { InputLabel } from "./InputLabel";
import { InputRoot } from "./InputRoot";

export const Input = {
  Root: InputRoot,
  Label: InputLabel,
  Error: InputError,
};
